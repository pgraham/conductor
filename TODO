- Update session manager to delete expired sessions. This needs to be
  re-evaluated.

- Move model parsing and ActorFactory out of clarinet and into a reed package.

- Update clarinet to have classes that implement database specific behaviour for
  supported databases.  Should support MySQL, PostgreSQL and SQLite
